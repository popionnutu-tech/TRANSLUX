/**
 * @translux/routing — общий движок маршрутов
 *
 * Чистая (pure-function) логика разбора, нормализации и сопоставления
 * tur/retur назначений. Используется и публичным сайтом, и админкой.
 *
 * Не содержит подключения к Supabase, не делает сетевых вызовов —
 * только функции которым передают уже загруженные данные.
 */

export * from './assignments.js';
export * from './stop-normalizer.js';
